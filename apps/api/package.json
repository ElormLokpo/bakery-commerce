{
  "name": "api",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "lint": "eslint . --ext .ts",
    "lint:check": "eslint . --ext .js,.ts",
    "type-check:local": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.11.3"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}